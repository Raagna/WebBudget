JWT_SECRET=replace-with-a-long-random-secret
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Optional: where the SQLite file lives. Defaults to backend/db/finance.db
# if unset - only needed when deploying somewhere with a specific mounted
# persistent-disk path, e.g. DB_PATH=/var/data/finance.db on Render.
# DB_PATH=
